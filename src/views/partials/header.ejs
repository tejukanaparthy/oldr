<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <!-- Google Fonts: Inter -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Global Styles */
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f7f9fc;
      color: #333;
    }
    /* Navigation Bar Styles */
    .navbar {
      background-color: #343a40;
      padding: 1rem;
    }
    .navbar-brand {
      font-weight: 600;
      font-size: 1.25rem;
      color: #fff;
    }
    .navbar .btn {
      font-weight: 500;
      font-size: 0.9rem;
    }
    .navbar .nav-user {
      font-size: 0.9rem;
      color: #bbb;
    }
    /* Main Container Styles */
    .container {
      max-width: 900px;
      margin: 50px auto;
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
    }
    /* Heading Styles */
    h1 {
      font-weight: 600;
      margin-bottom: 20px;
    }
    /* Button Styles */
    .btn-primary {
      background-color: #007bff;
      border: none;
    }
    .btn-primary:hover {
      background-color: #0056b3;
    }
    .btn-secondary {
      background-color: #6c757d;
      border: none;
    }
    .btn-secondary:hover {
      background-color: #5a6268;
    }
    /* Footer Styles */
    footer {
      text-align: center;
      margin-top: 50px;
      padding: 20px 0;
      background-color: #343a40;
      color: #fff;
    }
  </style>
</head>
<body>
  <!-- Navigation Bar -->
  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">OLDR</a>
      <div class="d-flex align-items-center">
        <% if (user) { %>
          <span class="me-3 nav-user">Logged in as: <%= user.firstname %> <%= user.lastname %></span>
          <a href="/api/users/welcome" class="btn btn-secondary btn-sm me-2">Home</a>
          <a href="/api/users/logout" class="btn btn-danger btn-sm">Logout</a>
        <% } else { %>
          <a href="/api/users/login" class="btn btn-primary btn-sm me-2">Login</a>
          <a href="/api/users/register" class="btn btn-success btn-sm">Register</a>
        <% } %>
      </div>
    </div>
  </nav>

  <!-- Main Content Container -->
  <div class="container mt-4">
    <h1><%= title %></h1>
